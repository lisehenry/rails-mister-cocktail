<ul>
  <% @cocktails.each do |cocktail| %>
    <li>
      <%= cocktail.name %>
      <%= link_to 'See detail', cocktail_path(cocktail.id), class: "btn btn-outline-primary" %>
    </li>
  <% end %>
</ul>
